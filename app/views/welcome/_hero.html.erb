<section class="hero">
  <div class="container">
    <h1>Welcome to Our Site</h1>
    <p>This is a hero section with a background image and a welcoming message for your visitors.</p>
    <a href="#" class="btn btn-custom">Get Started</a>
  </div>
</section>

<style>
  .hero {
    background: url('<%= asset_path("ruby.jpg") %>') no-repeat center center/cover;
    height: 100vh;
    width: 100vw;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: white;
  }

  .hero h1 {
    font-size: 3rem;
    font-weight: bold;
    margin-bottom: 20px;
  }

  .hero p {
    font-size: 1.25rem;
    margin-bottom: 30px;
  }

  .btn-custom {
    background: var(--tertiary-color);
    color: white;
    padding: 12px 30px;
    font-size: 1.1rem;
    font-weight: 600;
    border-radius: 50px;
    transition: all 0.3s ease;
  }

  .btn-custom:hover {
    background: var(--primary-color);
    color: white;
  }
</style>
